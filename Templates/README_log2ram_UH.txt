The directory log2ram can be deleted and newly created by "git clone https://github.com/azlux/log2ram.git".

No change was applied to the log2ram software. It is supplied as a snapshot without the directory ".git".

